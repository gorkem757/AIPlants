import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 24,
    maxHeight: 130,
  },
  contentContainer: { paddingHorizontal: 24, height: 130, columnGap: 12 },
});
